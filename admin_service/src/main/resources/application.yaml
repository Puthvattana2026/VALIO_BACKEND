server:
  port: 8086

spring:
  application:
    name: valio-admin-service

  profiles:
    active: default

  config:
    import: 
      - configserver:http://localhost:8083
      - optional:classpath:env.yaml