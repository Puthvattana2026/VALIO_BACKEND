server:
  port: 8085

spring:
  application:
    name: valio-auth-service

  profiles:
    active: default

  config:
    import:
      - configserver:http://localhost:8083
      - optional:classpath:env.yaml