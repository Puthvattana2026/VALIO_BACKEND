server:
  port: 8085

spring:
  application:
    name: valio-auth-service

  profiles:
    active: dev

  config:
    import:
      - configserver:http://localhost:8084
      - optional:classpath:env.yaml